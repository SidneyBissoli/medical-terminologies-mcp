; agents lista @modelcontextprotocol/* e zod como peers rígidos; sem esta flag o npm
; instalaria uma SEGUNDA cópia do SDK em worker/node_modules, quebrando o desenho de
; cópia única (o worker resolve SDK e zod do node_modules do pacote pai).
legacy-peer-deps=true
